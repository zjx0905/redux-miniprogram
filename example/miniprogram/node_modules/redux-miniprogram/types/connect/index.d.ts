import { MapStateToStore, MapDispatchToStore, ConnectType } from '../types';
export default function connect(type: ConnectType, mapStateToStore?: MapStateToStore, mapDispatchToStore?: MapDispatchToStore): (options: Record<string, any>) => Record<string, any>;
