import { AnyObject, MapStateToStore, MapDispatchToStore, ConnectPageOptions } from '../types';
/**
 * @param mapStateToStore 订阅 state
 * @param mapDispatchToStore 包装 dispatch
 */
export default function ConnectPage<TStore extends AnyObject>(mapStateToStore?: MapStateToStore, mapDispatchToStore?: MapDispatchToStore): <TData extends Record<string, any>, TCustom extends Record<string, any>>(options: ConnectPageOptions<TStore, TData, TCustom>) => ConnectPageOptions<TStore, TData, TCustom>;
