import { Store } from 'redux';
import { ProviderOptions } from '../types';
/**
 *
 * @param store Redux Store
 * @param options App Options
 */
export default function Provider<TStore extends Store>(store: TStore): <TObject extends Record<string, any>>(options: ProviderOptions<TStore, TObject>) => ProviderOptions<TStore, TObject>;
